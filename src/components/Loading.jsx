import React from 'react';

import './Loading.css';

const Loading = () => (
    <div className="loading">
        <div className="loading-card">
            <span>Cargando...</span>
        </div>
    </div>
);

export default Loading;